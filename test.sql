CREATE TABLE
    example_3(
        ID INT NOT NULL,
        NAME VARCHAR (20) NOT NULL,
        AGE INT NOT NULL,
        SALARY DECIMAL (18, 2),
        PRIMARY KEY (ID)
    ) CHARACTER set utf8mb4;